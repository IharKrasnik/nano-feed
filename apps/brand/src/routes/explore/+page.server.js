/** @type {import('./$types').PageServerLoad} */
export async function load({ params }) {
	return {
		ogTitle: 'Explore — Momentum Brand',
		ogDescription: 'Awesome content by Momentum creators',
		ogImage: ``
	};
}
